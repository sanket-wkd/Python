n = int (input("Enter the number "))
m = int(input("Enter the number "))
#note here no summ function is defined

if (n>m):
    n,m =m,n
    
# for i in range (n,m+1):
#     sum +=i
    
result = sum(range(n,m+1))
#r1 = sub(range(m , n+1))
    
print("the sum of number from ",n," to number ",m," is = ",result)
 #print(r1)

# not work for decimal numbers
'''
Output:-
Enter the number 1
Enter the number 3
the sum of number from  1  to number  3  is =  6

Enter the number 5
Enter the number 6
the sum of number from  5  to number  6  is =  11

Enter the number -1
Enter the number 1
the sum of number from  -1  to number  1  is =  0

Enter the number -1
Enter the number 2
the sum of number from  -1  to number  2  is =  2

Enter the number -1
Enter the number 3
the sum of number from  -1  to number  3  is =  5

Enter the number 0.1
Traceback (most recent call last):
'''
📌 Final takeaway
sum() exists → common & safe
sub() does NOT exist → ambiguous
Use - operator or define your own function

❌ Python does NOT auto-create math functions

Python only provides:
+ → addition
- → subtraction operator
* → multiplication
/ → division
There is no function for subtraction like sub() by default.

sum() DOES exist by default in Python

| Function | Exists? | Reason                               |
| -------- | ------- | ------------------------------------ |
| `sum()`  | ✅ Yes   | Addition is safe & order-independent |
| `sub()`  | ❌ No    | Subtraction depends on order         |


'''
